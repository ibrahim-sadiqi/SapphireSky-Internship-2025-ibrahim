// Minimum of Two Numbers
// Write a program that takes two numbers as input and prints the smaller number.

let fistNumber = Nu;

mber(prompt("Enter the first Number:"));
let secondNumber = Number(prompt("Enter the second number:"));

if (fistNumber > secondNumber) {
  console.log(`${fistNumber} is larger then ${secondNumber}`);
} else {
  console.log(`${secondNumber} is larger then ${fistNumber}`);
}
